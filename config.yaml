region: us-east-1

function_name: googl
handler: service.handler
role: lambda
description: Wrapper around Goo.gl URL Shortener service with retry logic 

# if access key and secret are left blank, boto will use the credentials
# defined in the [default] section of ~/.aws/credentials.
#aws_access_key_id: 
#aws_secret_access_key: 

# dist_directory: dist
timeout: 300
# memory_size: 512
